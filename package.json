{
  "name": "nasa.me",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn sass-build && elm-app build",
    "start": "elm-app start",
    "sass-watch": "sass --watch src/style.scss src/main.css",
    "sass-build": "sass src/style.scss src/main.css"
  },
  "dependencies": {
    "create-elm-app": "^3.0.6"
  },
  "devDependencies": {
    "sass": "^1.19.0"
  }
}
